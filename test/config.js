/* configuration for functional tests */

const config = {
    GEOPORTAL_WFS_URL: 'https://wxs.ign.fr/{apiKey}/geoportail/wfs',
    GEOPORTAL_API_KEY: 'choisirgeoportail',
    GEOPORTAL_REFERER: 'http://localhost'
};

module.exports = config;
